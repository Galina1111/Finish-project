import { FilterSerchPipe } from './filter-serch.pipe';

describe('FilterSerchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSerchPipe();
    expect(pipe).toBeTruthy();
  });
});
